import {
  autoReplyHeaders,
  buildForwardHtml,
  buildForwardSubject,
  getInternalReplyTo,
  sendEmail,
} from "../legacy";

export { autoReplyHeaders, buildForwardHtml, buildForwardSubject, getInternalReplyTo, sendEmail };
