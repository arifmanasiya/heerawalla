---
import { withBase } from '../lib/paths';
---

<header class="w-full sticky top-0 z-40 bg-white/95 backdrop-blur border-b border-slate-200/60">
  <div class="w-full px-6 pt-6 pb-2 flex items-center justify-between gap-6">
    <a href={withBase('/')} class="text-left">
      <span class="wordmark text-[16px] sm:text-[18px]">Heerawalla</span>
    </a>
    <nav class="hidden md:flex items-center space-x-8 text-[11px] uppercase tracking-[0.28em] text-slate-500" aria-label="Primary">
      <a href={withBase('/inspirations')} class="hover:text-ink">Inspirations</a>
      <a href={withBase('/contact?intent=consultation')} class="hover:text-ink">Concierge</a>
    </nav>
  </div>
  <nav
    class="md:hidden w-full px-6 pb-3 flex items-center space-x-6 text-[10px] uppercase tracking-[0.28em] text-slate-500"
    aria-label="Primary"
  >
    <a href={withBase('/inspirations')} class="hover:text-ink">Inspirations</a>
    <a href={withBase('/contact?intent=consultation')} class="hover:text-ink">Concierge</a>
  </nav>
</header>
